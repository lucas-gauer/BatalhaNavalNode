# BatalhaNavalNode

módulos npm usados:

	express

	ws

O servidor foi feito para ter um default e um backup. Para criar dois servidores é só copiar o servidor.js e modificar a variável "server" para o valor desejado.